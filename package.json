{
  "name": "cue",
  "version": "1.0.0",
  "description": "Capture-aware teleprompter overlay",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --inspect",
    "test": "node --test test/",
    "coverage": "c8 --reporter=text --reporter=html --include='lib/**/*.js' --include='server/index.js' node --test test/"
  },
  "dependencies": {
    "mongodb": "^7.2.0",
    "qrcode": "^1.5.3",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "c8": "^11.0.0",
    "electron": "^30.0.0"
  }
}
